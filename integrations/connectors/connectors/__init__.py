"""Concrete Connector implementations.

The runner imports each module here and registers its class. Adding a
new source means dropping a new file and adding it to REGISTRY in
runner.py — no other framework changes required.
"""

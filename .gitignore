node_modules/
.DS_Store
.planning/
supabase/

# Maintainer-local overlays (not part of upstream contribution contract)
.local/
.agent/
.claude.json

# Python caches
__pycache__/
*.pyc

# Credentials & runtime state
.env
.env.local
.env.*.local
# Per-profile env files generated by `make profile-init NAME=<name>` and the
# smoke-test-only .env.smoke generated by `make ci-env`.
.env.*
!.env.example
credentials.json
token.json
gmail-sync-log.json
sync-log.json

# Build artifacts
deno.lock
